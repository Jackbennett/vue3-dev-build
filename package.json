{
  "name": "bug-build-multiroot",
  "version": "1.0.0",
  "description": "test difference between dev and production output",
  "main": "index.js",
  "scripts": {
    "dev": "vite serve ./src",
    "build": "vite build --outDir=./public ./src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite": "^0.20.5"
  }
}
